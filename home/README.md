# muse-on.biz 網站原始碼（Cloudflare Pages 建置來源）

- Pages 專案：muse-on-biz-home｜root `home`｜build `npm run build`｜output `dist`｜Node 24.17.0
- 這個資料夾是「原始碼」，不是建好的網站；`dist/` 由 Cloudflare 自己建，不要 commit。
- 舊五頁與圖片在 `public/`，部落格文章在 `src/content/blog/`。
- 接線圖與發文步驟：本機工作正本 ~/muse-on-biz-site 的 docs/WIRING_MAP.md、docs/HOW_TO_ADD_BLOG_POST.md。
